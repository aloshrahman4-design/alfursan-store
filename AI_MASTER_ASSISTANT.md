# المساعد الرئيسي بالذكاء الاصطناعي (AI Master Assistant) -- وثيقة الإعداد

هذه الوثيقة تحدّد دور ونطاق "المساعد الرئيسي" الذي يُشغَّل عبر بوت
تيليجرام ومنصات الأتمتة (n8n / Make.com)، وكيف يتكامل مع الأنظمة
الموجودة فعلياً في هذا المستودع. الهدف: نص system prompt جاهز
للّصق في عقدة "AI Agent" بمنصة الأتمتة، مع توضيح صريح لما هو **داخل
النطاق فعلياً اليوم** مقابل ما هو **فكرة مستقبلية غير مُنفَّذة**.

## 1. النطاق الفعلي اليوم

المكوّنات القائمة فعلاً بالمستودع، وهي وحدها ما يجوز للمساعد الادعاء
بالتحكم بها أو التقرير عنها:

| المكوّن | الموقع | الوظيفة |
|---|---|---|
| بوت تسعير الصور | `telegram-bot/` | استقبال صور منتجات من الموردين، استخراج الأسعار عبر Gemini، تطبيق هامش الربح، تعديل الصورة، الرد بنفس المحادثة |
| واجهة المتجر | `index.html` | صفحة العرض والطلب للزبائن |
| لوحة الإدارة | `admin.html` | إدارة الطلبات والمخزون |
| صفحة تتبّع الطلب | `order-status.html` | عرض حالة الطلب للزبون |
| Firebase Functions | `functions/` | `createOrder`, `updateOrderStatus`, `checkRepPin`, `fbOAuthCallback`, `claimAutopostJob` (نشر تلقائي لفيسبوك) |

نص system prompt المقترح للمساعد (يُستخدم كما هو في عقدة الذكاء
الاصطناعي بـ n8n/Make):

```
أنت مساعد إدارة متجر الفرسان (أحذية، شحاطات، ملابس نسائية). مهمتك:
الرد على استفسارات المالك حول الطلبات (createOrder/updateOrderStatus)،
حالة بوت تسعير الصور، ونشر فيسبوك التلقائي -- بالاعتماد فقط على بيانات
حقيقية تصلك من Firebase أو من سجل بوت تيليجرام (audit_log.jsonl). لا
تخترع أرقام مبيعات أو حالات طلبات؛ إن لم تصلك بيانات فعلية قل ذلك
صراحةً. أي أمر يغيّر بيانات حقيقية (تعديل سعر، تغيير حالة طلب، نشر
منشور) يتطلب تأكيداً صريحاً من المالك قبل التنفيذ.
```

## 2. نمط ربط n8n / Make.com بالمكوّنات القائمة

الأتمتة تُبنى كطبقة **فوق** الأنظمة الحالية عبر Webhooks، لا كبديل لها:

```
تيليجرام (رسالة المالك) → n8n Webhook → عقدة AI Agent (system prompt أعلاه)
                                              │
                    ┌─────────────────────────┼─────────────────────────┐
                    ▼                         ▼                         ▼
         HTTPS → Firebase Functions   قراءة audit_log.jsonl      HTTPS → Graph API
        (createOrder/updateOrderStatus)   (سجل بوت التسعير)      (حالة نشر فيسبوك)
```

نقاط مهمة:

- لا يوجد اليوم Webhook عام مكشوف من `functions/` مخصص لاستقبال أوامر
  من n8n مباشرة -- أي دالة جديدة من هذا النوع يجب أن تتحقق من مصدر
  الطلب (مثال: `SHARED_SECRET` بترويسة الطلب، بنفس نمط `checkRepPin.js`
  و`claimAutopostJob.js` الحاليين) قبل تنفيذ أي تعديل.
- بوت تيليجرام (`telegram-bot/`) عملية مستقلة قائمة بذاتها (systemd) --
  ربطها بـ n8n يكون عبر قراءة `audit_log.jsonl` فقط (قراءة، لا كتابة)،
  حفاظاً على كون البوت نفسه هو المصدر الوحيد للحقيقة في حسابات الأسعار.

## 3. إدارة بيانات الاعتماد (Credentials) -- بدون تخزين فعلي بالمستودع

**لا يُخزَّن أي مفتاح أو توكن فعلي داخل git إطلاقاً** (نفس قاعدة قسم
"أمان وصلاحيات" بـ `telegram-bot/README.md`). النمط المعتمد:

1. **بيئة التطوير/الخادم**: متغيرات بيئة فقط، عبر `.env` (بوت تيليجرام،
   مستثنى بـ `.gitignore`) أو `EnvironmentFile=` بملف systemd. القيم
   الموجودة فعلاً: `TELEGRAM_BOT_TOKEN`, `GEMINI_API_KEY`,
   `ALLOWED_USER_IDS`.
2. **Firebase Functions**: أسرار عبر `firebase functions:secrets:set`
   (Secret Manager)، لا عبر ملفات بالمستودع.
3. **منصات الأتمتة (n8n/Make)**: بيانات الاعتماد تُخزَّن داخل "Credentials
   Store" الخاص بالمنصة نفسها (مشفّرة عندها)، ويُشار لها بالاسم فقط
   داخل الـ workflow، لا بالقيمة.
4. **الانتهاء والتجديد**: أي خطوة تحتاج تجديد توكن (مثال: توكن صفحة
   فيسبوك طويل الأمد عبر `fbOAuthCallback.js`) يجب أن تُسجَّل تلقائياً
   تاريخ الانتهاء المرتجع من المنصة، وتُنشئ تنبيهاً (رسالة تيليجرام
   للمالك) قبل الانتهاء بفترة كافية -- لا "تجديد صامت" يغيّر بيانات
   الحساب دون علم المالك.

## 4. خارج النطاق حالياً (أفكار غير مُنفَّذة)

البنود التالية **لا يوجد لها أي كود بهذا المستودع اليوم**، وتم استبعادها
عمداً من هذا الإعداد لأنها تتطلب صلاحيات حساسة (تحكم بجهاز فعلي، تنفيذ
عمليات مالية حقيقية) لا يجوز تفعيلها دون قرار وتصميم أمان منفصل ومباشر
من المالك:

- **التحكم بالهاتف (Samsung S23 Ultra)**: لا يوجد تكامل حالياً مع أي
  أداة أتمتة أجهزة (مثال: Tasker/ADB عبر n8n). أي إضافة مستقبلية يجب أن
  تحدد بدقة الأوامر المسموحة فقط (قائمة بيضاء صريحة)، لا تنفيذاً حراً
  لأي أمر نصي.
- **التداول المالي (Trading)**: لا يوجد كود اتصال بأي منصة تداول أو
  بيانات سوق. أي تنفيذ آلي لصفقات حقيقية يتطلب على الأقل: بيئة تجريبية
  (paper trading) للاختبار أولاً، حدود مخاطرة صريحة، وتأكيد يدوي قبل أي
  صفقة حقيقية -- لا تُفعَّل هذه الوثيقة وحدها أي تنفيذ من هذا النوع.

عند الرغبة الفعلية بتفعيل أي من هذين البندين، يلزم طلب منفصل يحدد
بالضبط: الأداة/API المستهدفة، حدود الصلاحية، وآلية التأكيد قبل التنفيذ.
